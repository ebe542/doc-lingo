# A small translation example

The **classification model** processes *new observations* and predicts a category.
Read the [user guide](https://example.org/guide "User guide") before starting.

## Instructions

- Prepare the input data and check the column names
  before running the classification model.
- Keep the original file for comparison.

1. Open the document.
2. Review the translated result.

> A prediction is not a guarantee.

Use `model.predict(data)` to request a prediction.

```python
message = "This code must remain unchanged."
```

| Name | Value |
| --- | --- |
| Sample | 12 |

<div>This HTML block remains unchanged.</div>
